FROM node:22-alpine
WORKDIR /app
COPY package.json package-lock.json* ./
RUN if [ -f package-lock.json ]; then npm ci --ignore-scripts; else npm install --ignore-scripts; fi
COPY . .
RUN npm rebuild 2>/dev/null || true
EXPOSE 5173 __BACKEND_PORT__
CMD ["npm", "run", "dev"]
